# Cloud Build configuration for GKE deployment
# Usage: gcloud builds submit --config cloudbuild.yaml

steps:
  # Build the Node.js Docker image
  - name: 'gcr.io/cloud-builders/docker'
    args:
      - 'build'
      - '-t'
      - 'gcr.io/$PROJECT_ID/chaincopilot:$COMMIT_SHA'
      - '-t'
      - 'gcr.io/$PROJECT_ID/chaincopilot:latest'
      - '.'

  # Build the Python sidecar Docker image
  - name: 'gcr.io/cloud-builders/docker'
    args:
      - 'build'
      - '-f'
      - 'Dockerfile.python'
      - '-t'
      - 'gcr.io/$PROJECT_ID/chaincopilot-python:$COMMIT_SHA'
      - '-t'
      - 'gcr.io/$PROJECT_ID/chaincopilot-python:latest'
      - '.'

  # Push Node.js images
  - name: 'gcr.io/cloud-builders/docker'
    args:
      - 'push'
      - 'gcr.io/$PROJECT_ID/chaincopilot:$COMMIT_SHA'

  - name: 'gcr.io/cloud-builders/docker'
    args:
      - 'push'
      - 'gcr.io/$PROJECT_ID/chaincopilot:latest'

  # Push Python sidecar images
  - name: 'gcr.io/cloud-builders/docker'
    args:
      - 'push'
      - 'gcr.io/$PROJECT_ID/chaincopilot-python:$COMMIT_SHA'

  - name: 'gcr.io/cloud-builders/docker'
    args:
      - 'push'
      - 'gcr.io/$PROJECT_ID/chaincopilot-python:latest'

  # Deploy to GKE
  - name: 'gcr.io/cloud-builders/gke-deploy'
    args:
      - 'run'
      - '--filename=k8s/deployment.yaml'
      - '--image=gcr.io/$PROJECT_ID/chaincopilot:$COMMIT_SHA'
      - '--location=${_GKE_LOCATION}'
      - '--cluster=${_GKE_CLUSTER}'

substitutions:
  _GKE_CLUSTER: chaincopilot-cluster
  _GKE_LOCATION: us-central1

images:
  - 'gcr.io/$PROJECT_ID/chaincopilot:$COMMIT_SHA'
  - 'gcr.io/$PROJECT_ID/chaincopilot:latest'
  - 'gcr.io/$PROJECT_ID/chaincopilot-python:$COMMIT_SHA'
  - 'gcr.io/$PROJECT_ID/chaincopilot-python:latest'

options:
  logging: CLOUD_LOGGING_ONLY
